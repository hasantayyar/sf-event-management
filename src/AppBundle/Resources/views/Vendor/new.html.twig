{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Vendor creation</h1>

    {{ form(form) }}

    <hr>
    <a href="{{ path('admin_vendor') }}" class="btn btn-info">
        {{"Back to the list"|trans}}
    </a>

{% endblock %}
