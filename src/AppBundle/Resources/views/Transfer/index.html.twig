{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Transfer list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Destinationpointid</th>
                <th>Departurepointid</th>
                <th>Venueid</th>
                <th>Carid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_transfer_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.destinationPointId }}</td>
                <td>{{ entity.departurePointId }}</td>
                <td>{{ entity.venueId }}</td>
                <td>{{ entity.carId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_transfer_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_transfer_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_transfer_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
