{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Transfer edit</h1>

    {{ form(edit_form) }}


    <a href="{{ path('admin_transfer') }}">
        Back to the list
    </a>

    <hr>{{ form(delete_form) }}

{% endblock %}
