{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Venue Hall creation</h1>

    {{ form(form) }}


    <a class="btn btn-default" href="{{ path('admin_venue_hall') }}">
        Back to the list
    </a>

{% endblock %}
