{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Venue Hall edit</h1>

    {{ form(edit_form) }}

    <a href="{{ path('admin_venue_hall') }}">
        Back to the list
    </a>

    <hr>
    {{ form(delete_form) }}

{% endblock %}
