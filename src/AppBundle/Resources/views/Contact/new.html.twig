{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Contact creation</h1>

    {{ form(form) }}


    <a  class="btn btn-default" href="{{ path('admin_contact') }}">
        Back to the list
    </a>

{% endblock %}
