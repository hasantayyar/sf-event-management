{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>{{"Contact edit"|trans}}</h1>

    {{ form(edit_form) }}


    <a class="btn btn-default" href="{{ path('admin_contact') }}">
        {{" Back to the list"|trans}}
    </a>
    <hr>
    {{ form(delete_form) }}

{% endblock %}
