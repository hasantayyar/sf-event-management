{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>TransferPoint edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_transfer_points') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
