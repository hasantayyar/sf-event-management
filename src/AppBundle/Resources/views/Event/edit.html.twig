{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Event edit</h1>

    {{ form(edit_form) }} 

    <a href="{{ path('admin_event') }}">
        Back to the list
    </a>
    <hr>
    {{ form(delete_form) }}

{% endblock %}
