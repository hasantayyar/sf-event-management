{% extends '::base_admin.html.twig' %}

{% block content -%}
    <h1>Venue creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_venue') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
